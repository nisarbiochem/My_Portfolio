// src/components/Navbar.jsx

const Navbar = () => {
  return (
    <nav>
      <h1>My Portfolio</h1>
      {/* Add your navbar links, etc. */}
    </nav>
  );
};

export default Navbar;     // ← Must be at the BOTTOM